<div class="top-row ps-3 navbar navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="">AppDIGESETT - Agente</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/modulo-agente/registrar-multa" Match="NavLinkMatch.All">
                <span class="bi bi-plus-square-fill"></span> Registrar Multa
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/modulo-agente/listado-multas">
                <span class="bi bi-list-ul"></span> Listado de Multas
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/modulo-agente/mapa-multas">
                <span class="bi bi-geo-alt"></span> Mapa con Mis Multas
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/modulo-agente/comision-por-mes">
                <span class="bi bi-cash-coin"></span> Comisión por Mes
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/modulo-agente/acerca-de">
                <span class="bi bi-info-circle"></span> Acerca de
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <button class="btn btn-danger w-100 mt-2" @onclick="() => Nav.NavigateTo(Login)">Salir</button>
        </div>
    </nav>
</div>

@code {
    [Inject] NavigationManager Nav { get; set; }
    private bool collapseNavMenu = true;

    public const string Login = "/";

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
